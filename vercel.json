{
  "framework": null,
  "installCommand": "cd backend && pnpm install --frozen-lockfile",
  "buildCommand": "cd backend && pnpm run build:vercel && mkdir -p ../api && cp api/index.js ../api/index.js",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}
