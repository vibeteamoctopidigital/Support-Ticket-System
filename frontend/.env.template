# Base URL of the backend API — must include the /api prefix.
# Local dev:
NEXT_PUBLIC_API_URL="http://localhost:8080/api"
# Production (backend is deployed on Railway, not Vercel):
#   NEXT_PUBLIC_API_URL="https://tender-unity-production-282f.up.railway.app/api"
